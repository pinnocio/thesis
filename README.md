This repo is shared between Claude Code and Codex.

Shared behavior lives in `AGENTS.md`. Claude-specific automation lives in `.claude/`; Codex should follow the explicit Codex instructions in `AGENTS.md`, including manually reading the relevant context file before section 1.1 work.
